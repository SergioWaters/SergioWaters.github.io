export const activateScroll = () => {
  // const projects = document.querySelectorAll(".project");
  // let prevPosition = 0;
  // let isScrollDown = true;
  // let active = 0;
  // window.addEventListener("scroll", checkScrollPosition);
  // function checkScrollPosition() {
  //   if (active < 0) active = 0;
  //   if (active > projects.length) active = projects.length;
  //   const currPosition = window.pageYOffset || window.scrollY;
  //   currPosition > prevPosition
  //     ? (isScrollDown = true)
  //     : (isScrollDown = false);
  //   prevPosition = currPosition;
  //   if (projects[active + 1].getBoundingClientRect().top < 900) {
  //     projects[active + 1].scrollIntoView({
  //       block: "start",
  //       behavior: "smooth",
  //     });
  //   }
  //   projects[active + 1].style.border = "1px solid red";
  //   console.log(
  //     projects[active + 1].getBoundingClientRect().top,
  //     "window-top - " + currPosition
  //   );
  // }
};
